// Package geom provides the basic geometry primitives shared by figures,
// transforms, renderers, and backend implementations.
//
// The package intentionally stays small: points, rectangles, paths, and affine
// transforms. These types are the stable way for applications and external
// backends to exchange geometry with matplotlib-go.
package geom
