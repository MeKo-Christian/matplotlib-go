version = "2"

[run]
timeout = "5m"
tests = true

[linters]
default = "standard"
enable = [
  "misspell",
  "gocritic",
    "revive"
]
disable = [
    "depguard",
    "errcheck",
]

[linters.exclusions]
generated = "strict"

[[linters.exclusions.rules]]
# safe to ignore in initialization code
linters = ["errcheck"]
text = "viper.Bind"

[[linters.exclusions.rules]]
# idiomatic viper usage
path = "cmd/.*\\.go"
linters = ["gochecknoinits", "gochecknoglobals"]

[[linters.exclusions.rules]]
# The tri package keeps value semantics on purpose: Triangulation is four slice
# headers (value receivers match the matplotlib-style API and the frozen public
# signatures), and the cubic-element math passes small fixed-size matrices by
# value for readability in tight numerical loops.
path = "tri/.*\\.go"
linters = ["gocritic"]
text = "hugeParam"

[linters.settings]

[linters.settings.gocritic]
enabled-tags = ["diagnostic", "style", "performance"]
disabled-checks = ["commentedOutCode", "unnamedResult", "importShadow", "dupImport", "dupSubExpr"]
[linters.settings.revive]
[[linters.settings.revive.rules]]
name = "file-length-limit"

[[linters.settings.revive.rules.arguments]]
max = 1500
skip-comments = false
skip-blank-lines = false
